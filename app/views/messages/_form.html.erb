<%= form_for @visitor_message, url: messages_url do |f| %>
      <% if @visitor_message.errors.any?%>
            <div id="error_explanation">
                <h2><%=pluralize(@visitor_message.errors.count, "error")%>prohibited this message from sendding:</h2>
                <ul>
                      <%= @visitor_message.errors.full_messages.each do |message|%>
                        <li><%=message%></li>
                      <%end%>
                </ul>
            </div>
      <%end%>
    <h3>Send me a message</h3>
    <p>
      <label class="sr-only">Your Name</label>
      <%= f.text_field :fullname, class:"form-control", placeholder:"Your Name"%>
    </p>
   <p>
      <label class="sr-only">Email Address</label>
       <%= f.email_field :email,class:"form-control", placeholder:"Email Address"%>
    </p>
        <%=f.fields_for :messages do |message| %>
            <p>
              <label class="sr-only">Your Message</label>
              <%= message.text_area :content, class:"form-control", placeholder:"Your Message", id:"textarea" %>
            </p>
        <%end%>
<p><%= f.submit "Send Message", class:"btn btn-primary"%></p>
<%end%>
